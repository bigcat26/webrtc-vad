{
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/.build --parallel --verbose"
        },
    ],
    "version": "2.0.0"
}
